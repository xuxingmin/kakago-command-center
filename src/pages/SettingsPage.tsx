import { Settings } from "lucide-react";
import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function SettingsPage() {
  return <PagePlaceholder title="设置模块" icon={Settings} />;
}
