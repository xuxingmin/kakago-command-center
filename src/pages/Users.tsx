import { Users as UsersIcon } from "lucide-react";
import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function Users() {
  return <PagePlaceholder title="用户中心模块" icon={UsersIcon} />;
}
